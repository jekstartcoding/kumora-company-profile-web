VITE_SUPABASE_URL=              # "API URL" project Supabase
VITE_SUPABASE_ANON_KEY=         # anon public key — read-only (RLS Fase 2: hanya SELECT)
VITE_ADMIN_API_URL=            # base URL backend (dev: http://localhost:3000, production: URL Railway)
VITE_SUPABASE_PUBLISHABLE_KEY= # publishable key — untuk LOGIN admin panel (auth Supabase)
